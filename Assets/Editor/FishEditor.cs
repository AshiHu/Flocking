using UnityEngine;
using UnityEditor;

/*[CustomEditor(typeof(Fish))]
public class FishEditor : Editor
{
    base.OnInspectorGUI();

    serializedProperty serializedata = serializedObject.FindProperty("data");

    object data = serializedata.objectReferenceValue;

    FishEditor.createCachedEditor(data, null, reff fishData);
}
*/